# Shared fixtures

Canonical fixtures live in `demo/` (research_grant, reimbursement).
Benchmark fixtures live in `benchmark/processpatchbench/cases/` (26 scenarios).
JSON contracts live in `shared/schemas/`. Typed entities in `shared/models/`.
