# agents.md — Multi-Agent Coordination Protocol

This repo is worked on by **three AI agents** (plus the human owner, Aman, who
is the only one who commits/pushes). This file prevents conflicts and keeps a
durable record of who did what.

**Rule zero: read this file before starting any task. Append a claim before
touching a file. Append a log entry when you stop.**

---

## 1. Agent roster

| Agent | Capabilities | Default ownership |
|---|---|---|
| **Buffy** (Freebuff/Codebuff) | Full repo, runs tests/verify/terminal, no web search | `services/**`, `tests/**`, `scripts/**`, `infra/**`, `benchmark/**`, root config |
| **opencode** | Code edits + **web search** (docs, design references) | `frontend/**` (while a UI task is open) |
| **IBM Bob** | Code edits + analysis, **no web access** | Read-only reviews, new standalone test files, docs, CHANGELOG |

The human (Aman) reviews all work and is the only committer. Agents never
commit, push, or deploy.

## 2. Conflict prevention (the important part)

- **One file, one agent, at a time.** Before editing, add a *claim* to the
  Task Board (section 4): your name, task id, and the exact paths you will
  touch. If another agent has an open claim on those paths, **stop and pick a
  different task** (or wait until the claim is released).
- **Never edit outside your claimed paths**, even for "tiny fixes" — log the
  need as a note on the task board instead.
- Cross-cutting files (`README.md`, `agents.md`): factual log/appending is
  always allowed; structural edits need a claim.
- Keep tasks **small**: claim → do → verify → log → release. Long-lived claims
  are how conflicts happen.
- If you must break these rules (emergency), log it loudly in section 6 with
  `!!` prefix and tell the human.

## 3. Definition of Done (every task)

1. All claimed changes complete; no drive-by edits elsewhere.
2. `python scripts/verify.py` → PASS and `python -m pytest tests -q` → all
   green (agents without terminal access: state this in the log and request
   the human/Buffy to run verification).
3. `scripts/validate_infra.py` → PASS (only if `infra/**` was touched).
4. Log entry written (section 6) and claim released (section 4).
5. A one-line commit message proposal included in the log entry.

## 4. Task board

Format: `T<id> | <title> | owner | paths | state (OPEN / CLAIMED / DONE / BLOCKED)`

| ID | Title | Owner | Paths | State |
|---|---|---|---|---|
| T1 | Complete UI upgrade (design-system pass, anti-slop rules — prompt provided separately) | **opencode** | `frontend/**` | DONE |
| T2 | Docs consistency audit (stale counts, broken cross-refs after v0.3 auth/traces/workspaces changes) | **IBM Bob** | `docs/**`, `README.md`, `docs/consistency-audit.md` (new) | DONE |
| T3 | New unit tests for `services/normalizer/normalizer.py` interval + delta edge cases | **IBM Bob** | `tests/unit/test_normalizer_ext.py` (new file only) | DONE |
| T4 | `CHANGELOG.md` for v0.3.0 features (auth, traces, language, workspaces, judge demo) | **IBM Bob** | `CHANGELOG.md` (new file only) | DONE |
| T5 | Backend standing by: fixes/reviews requested by T1–T4; no frontend edits | **Buffy** | everything except `frontend/**` | STANDBY |
| T6 | One-command local app (`make app`: UI + same-origin API proxy on one port) | **Buffy** | `scripts/app.py` (new), `Makefile`, 1 line in `frontend/lib/app.js` | DONE |
| T7 | Seeded demo state + app E2E suite + CSV bulk trace ingest | **Buffy** | `scripts/app.py`, `services/traces/`, `services/api/{actions,server}.py`, `services/aws_handlers.py`, `tests/**`, `docs/traces.md`, `CHANGELOG.md` | DONE |
| T8 | First-run experience polish (empty states + no-build hero) | **opencode** | `frontend/**` | DONE |
| T9 | Demo-script rewrite (make app reality) + runbook step 0 + CHANGELOG v0.3.1 | **opencode** | `docs/demo-script.md`, `docs/deployment-runbook.md`, `CHANGELOG.md` | DONE |
| T10 | Visual overhaul pass 2 (depth, hero, tables, graph colors, approval pills) | **Buffy** | `frontend/**` | DONE |
| T11 | Judge-prep research: docs/positioning.md + docs/judge-qa.md | **opencode** | `docs/positioning.md` (new), `docs/judge-qa.md` (new) | DONE (duplicate row; see below) |
| T16 | Benchmark v0.4 case-family proposals (grounded in rule-ir + author.py) | **opencode** | `docs/benchmark-v4-proposals.md` (new) | DONE |
| T17 | Demo storyboard (180s shot plan) + submission.md refresh | **opencode** | `docs/demo-storyboard.md` (new), `docs/submission.md` (edit) | DONE |
| T12 | Design-system v3 (Satoshi + Phosphor self-hosted, gate spine, case-file witnesses, sentence-case labels, theme toggle, icon nav/empty states) | **Buffy** | `frontend/**` | DONE |
| T13 | Filled dashboard canvas (activity modules, editor panel, header hierarchy, sidebar polish, spine pulse) | **Buffy** | `frontend/**` | DONE |
| T14 | Identity v4 "ledger of record" (warm paper default + deep-teal seal accent; graphite+muted-cyan dark alternate) | **Buffy** | `frontend/**` | DONE |
| T15 | Governance bundle export (sha256-sealed evidence pack, 409 without approval) + witness nomination (honesty gate → verified pipeline) + CSV ingest UI | **Buffy** | `services/api/**`, `services/aws_handlers.py`, `services/api/authz.py`, `frontend/lib/app.js`, `docs/traces.md`, `CHANGELOG.md`, tests | DONE |
| T16 | Theme cycle (paper → graphite → lab), hero asymmetry 1.35fr, drag-drop policy editor, deleted duplicate tagline; external design review triaged (stale screenshot — concepts 1/3 already shipped as T14 tokens) | **Buffy** | `frontend/**` | DONE |
| T16b | Lab (rust/graphite) promoted to DEFAULT theme; cycle now lab → paper → graphite → lab; no-flash boot hint in index.html | **Buffy** | `frontend/**` | DONE |
| T11 | Positioning + judge Q&A docs (web research + repo-grounded) | **opencode** | `docs/positioning.md` (new), `docs/judge-qa.md` (new) | DONE |
| T18 | Command palette (Ctrl/Cmd+K) + coverage endpoint `GET /builds/{id}/coverage` (BOTH surfaces) + Impact coverage panel | **Buffy** (reassigned from opencode per human) | `frontend/**`, `services/api/actions.py`, `services/api/server.py`, `services/aws_handlers.py`, tests | DONE |
| T19 | T15 governance docs sweep (bundle + nomination in README/architecture; number sweep) | **opencode** | `README.md`, `docs/architecture.md`, `docs/submission.md` (numbers block only) | DONE |
| T20 | CSV bulk-ingest demo sample + storyboard paste moment | **opencode** | `demo/traces-sample.csv` (new), `docs/demo-storyboard.md` (3-line edit) | DONE |
| T21 | Judge QA + positioning refresh (bundle, nomination, coverage) | **opencode** | `docs/judge-qa.md` (edit), `docs/positioning.md` (edit) | DONE |
| T25 | Live deploy audit (Amplify URL) + fix runbook + judge checklist | **opencode** | `docs/deploy-status.md` (new) | DONE |
| T22 (was T6 on deploy branch) | Repair GitHub Actions AWS OIDC deployment authentication | **GitHub Copilot** | `.github/workflows/ci.yml`, `docs/aws-setup.md`, `agents.md` | DONE |
| T23 (was T7 on deploy branch) | Diagnose recurring GitHub Actions OIDC trust rejection | **GitHub Copilot** | `docs/aws-setup.md`, `agents.md` | DONE |
| T24 (was T8 on deploy branch) | Eliminate GitHub OIDC role ARN secret mismatch | **GitHub Copilot** | `.github/workflows/ci.yml`, `docs/aws-setup.md`, `agents.md` | DONE |
| T27 (was T9 on deploy branch) | Add one-run GitHub OIDC claim diagnostics | **GitHub Copilot** | `.github/workflows/ci.yml`, `docs/aws-setup.md`, `agents.md` | DONE |
| T28 (was T10 on deploy branch) | Support GitHub immutable OIDC subject claims | **GitHub Copilot** | `docs/aws-setup.md`, `agents.md` | DONE |
| T33 | Deploy-demo blockers: API CORS allow-list hardcoded to include the LIVE amplify URL (deploy.sh discovery had silently deployed FrontendOrigin=main.dummy.amplifyapp.com — every real browser CORS-blocked); Cognito callbacks + localhost:8080; demo-admin user via IaC (invite-only pool had NO login); loud FrontendOrigin discovery with FRONTEND_ORIGIN override (no silent dummy); print-once demo password in deploy.sh | **Buffy** | `infra/template.yaml`, `infra/deploy.sh`, `agents.md` | DONE |
| T35 | Cloud E2E smoke: scripts/cloud_smoke.sh — password grant → Bearer → deferred compile → execute → poll → Gates 1/2/3 via /resume → bundle + coverage, exit-code-mapped; verified against LIVE API (config fetch OK, login path live, wrong-pass → clean exit 2). Deploy-side verification of T33: CORS now echoes the LIVE origin (was dummy-only), callback whitelist enforced (evil origin → redirect_mismatch, ours → login page) | **Buffy** | `scripts/cloud_smoke.sh` (new), `agents.md` | DONE |
| T37 | CI deploy exit-3 fix: Amplify app is repo-named (Bharatbuilds), not stack-named (processpatch-demo) — name lookup could never match, loud guard (correctly) refused to guess. deploy.sh discovery now: FRONTEND_ORIGIN env → name match → single-amplifyapp-domain scan → loud exit; CI pins FRONTEND_ORIGIN so deploys are deterministic. NOTE: pass 1 of the failing run SUCCEEDED — CORS allow-list + DemoAdmin are live; only the redundant auth-wiring pass aborted | **Buffy** | `infra/deploy.sh`, `.github/workflows/ci.yml`, `agents.md` | DONE |
| T38 | DEMO-BLOCKER found by first real cloud login: API authorizer required OAuth scopes (AuthorizationScopes [openid,email]) but Cognito ID tokens NEVER carry a scope claim → every authenticated caller 403s, including the browser flow (masked until now because nobody could log in). Fix: drop AuthorizationScopes; audience+issuer remain the gate. cloud_smoke.sh: login moved from /oauth2/token (code-flow only → unsupported_grant_type) to InitiateAuth USER_PASSWORD_AUTH with ID/access probe fallback | **Buffy** | `infra/template.yaml`, `scripts/cloud_smoke.sh`, `agents.md` | DONE |
| T39 | CORS dedupe: T37's CI-pinned FrontendOrigin == T33's hardcoded allow-origin → ApiGatewayV2 "Duplicated values are not allowed in allow-origins" → T38 deploy rolled back (scope fix withheld too). Template now hardcodes ONLY localhost (cannot collide); FrontendOrigin is the single source of truth for the live URL | **Buffy** | `infra/template.yaml` | DONE |
| T40 | Password rotation killed: deploy.sh's demo-admin block rotated the password on EVERY deploy — the green T39 deploy silently clobbered the human-set password (smoke → NotAuthorizedException). deploy.sh now NEVER rotates: optional DEMO_PASSWORD env applies explicitly, otherwise untouched | **Buffy** | `infra/deploy.sh`, `agents.md` | DONE |
| T41 | WRITE-BLOCKER found by first successful cloud login: HTTP API JWT authorizer drops colon-carrying claims (cognito:groups) before the Lambda — token had pp-admins but role collapsed to READ_ONLY, every write 403'd (GET worked: reads are all-role). Fix: claims_from_event re-derives identity from the Bearer token via full stdlib RS256 verify (cached JWKS) when authorizer claims are group-less; secure default (readonly) on missing/malformed header (ValueError catch — binascii.Error). Proven by RSA sign→verify roundtrip + negative controls; 4 regression tests. Smoke also fixed: /oauth2/token→unsupported_grant_type was code-flow-only → InitiateAuth USER_PASSWORD_AUTH; Windows /tmp + pipefail gotchas | **Buffy** | `services/api/authz.py`, `scripts/cloud_smoke.sh`, `tests/unit/test_authz_fallback.py` (new), `agents.md` | DONE |
| T29 (user-labeled T27; T27 taken) | Presenter runbook: timed 4-min demo script + README quick-start verify | **opencode** | `docs/demo-script.md` (rewrite), `README.md` (quick-start block only) | DONE |
| T34 | Deployed demo login docs (auth.md section + demo-script sign-in beat) | **opencode** | `docs/auth.md` (edit), `docs/demo-script.md` (2-line insert) | DONE |
| T36 | Deploy-status refresh: doom-report → status page (live re-audit) | **opencode** | `docs/deploy-status.md` (edit) | DONE |
| T29 (was T11 on deploy branch) | Fix Cognito bootstrap callback URL deployment | **GitHub Copilot** | `infra/template.yaml`, `infra/deploy.sh`, `docs/aws-setup.md`, `agents.md` | DONE |
| T30 (was T12 on deploy branch) | Pass HTTPS bootstrap origin on first deploy | **GitHub Copilot** | `infra/deploy.sh`, `infra/parameters.json`, `docs/aws-setup.md`, `agents.md` | DONE |
| T31 (was T13 on deploy branch) | Wire Amplify token into GitHub deployment | **GitHub Copilot** | `.github/workflows/ci.yml`, `infra/deploy.sh`, `docs/aws-setup.md`, `agents.md` | DONE |
| T32 (was T14 on deploy branch) | Harden second SAM deployment pass | **GitHub Copilot** | `infra/deploy.sh`, `docs/aws-setup.md`, `agents.md` | DONE |

| T41 | Production readiness audit, API hardening and UI usability | Codex | `services/api/authz.py`, `services/storage.py`, `infra/template.yaml`, `frontend/index.html`, `frontend/styles.css`, `frontend/lib/app.js`, `tests/unit/test_readiness.py` (new), `services/api/actions.py`, `services/api/server.py`, `services/aws_handlers.py`, `docs/production-readiness.md` (new) | DONE |

| T42 | Remove Judge Mode from the UI | Codex | `frontend/index.html`, `frontend/lib/app.js`, `frontend/styles.css`, `frontend/DESIGN.md` | DONE |

| T43 | Make pre-compile tabs navigable with honest empty states | Codex | `frontend/index.html`, `frontend/lib/app.js`, `frontend/styles.css`, `agents.md` | DONE |
| T47 | Fix frontend auth race/alert loop and restore pre-compile tab access | Buffy | `frontend/lib/app.js`, `frontend/index.html`, `agents.md` | DONE |
| T48 | `send_task_success(output=)` kwarg fix — SFN gates actually un-wait | Buffy | `services/governance/store.py`, `agents.md` | DONE |
| T49 | `describe_execution` surfaces SFN error/cause for failed runs | Buffy | `services/api/actions.py`, `agents.md` | DONE |
| T50 | DynamoDB duplicate-SK fix: gate callbacks keyed `build_id#gate` | Buffy | `services/storage.py`, `services/api/actions.py`, `tests/unit/test_t50_callback_keys.py`, `agents.md` | DONE (deployed) |
| T51 | Activation gate emits reviewer/reason per the ACTIVATE ASL contract | Buffy | `services/governance/store.py`, `tests/unit/test_t51_activation_payload.py`, `agents.md` | DONE (deployed) |
| T52 | Full 3-gate cloud E2E green — first activated cloud build | Buffy | `agents.md` (log only) | DONE |
| T53 | ASL gate-contract tests (all three gates, fields derived from the ASL) + mutation-verified | Buffy | `tests/unit/test_asl_gate_contracts.py` (new), `agents.md` | READY TO COMMIT |
| T54 | Build archival (non-destructive cleanup) on both API surfaces + live cleanup script | Buffy | `services/registry/store.py`, `services/api/{actions,server}.py`, `services/aws_handlers.py`, `scripts/demo_cleanup.py` (new), `tests/unit/test_build_archive.py` (new), `tests/unit/test_readiness.py` (stub arity), `agents.md` | READY TO COMMIT |
| T55 | Flagged hard-purge route (demo resets only): PP_DEMO_PURGE, admin-only, audited before deletion | Buffy | `services/registry/store.py`, `services/governance/store.py`, `services/api/{actions,server,authz}.py`, `services/aws_handlers.py`, `scripts/demo_cleanup.py`, `tests/unit/test_build_purge.py` (new), `infra/template.yaml` | READY TO COMMIT |
| T56 | Live demo driver: fresh amendment → compile → all three gates → evidence (rehearsal path, proven live) | Buffy | `scripts/demo_run.py` (new), `agents.md` | READY TO COMMIT |
| T57 | Env-flag parsing fix + deploy-switchable AuthDebug/DemoPurge parameters | Buffy | `services/aws_handlers.py`, `infra/template.yaml`, `agents.md` | READY TO COMMIT |

Backlog (unassigned): Cognito MFA toggle, benchmark v0.4 families, SFN ASL
tests for auth-related resume gates, `POST /traces/csv` UI affordance
(backend done; needs an ingest-CSV button next to the trace form).

**T1 special rule:** while T1 is CLAIMED, Buffy and Bob must not edit
`frontend/**`. Buffy may *read* it for review. The UI↔API contract must not
change (see T1 notes below); if opencode needs a backend change, it logs a
`NEEDS_BACKEND` note and stops that part — Buffy implements it.

## 5. Facts every agent needs (do not re-derive)

- Repo: ProcessPatch — operational rule compiler; docs in `docs/` (16 notes);
  session history in `ProcessPatch_Session_Context.md` (repo root, never committed).
- Verification is credential-free: `python scripts/verify.py` (178 assertions),
  `pytest tests` (52), `python scripts/validate_infra.py`.
- Benchmark is **v0.3.0, 31 cases** (22 auto-repaired / 3 no-op / 4 escalated /
  2 unsupported). Never hardcode metric counts; compute them.
- Auth: Cognito groups `pp-admins` (activate) / `pp-reviewers` (review) —
  `services/api/authz.py` stamps verified identity; `docs/auth.md`.
- Doctrine: ambiguity → NEEDS_REVIEW; no witness without verified pipeline;
  never "compliance guaranteed"; traces are evidence only (`docs/traces.md`).
- The human commits. Propose commit messages; never run git commit/push.

## 6. Activity log (append-only, newest first)

- 2026-09-20 04:30 | Buffy | T47 frontend auth race & 401 alert loop fix + pre-compile tab access | `frontend/lib/app.js`, `frontend/index.html`, `agents.md` | DONE: sequenced startup (authLoad -> direct auth/config fetch -> await authMaybeExchange -> renderTabs -> render -> loadHistory), eliminated blocking 401 alert loop from get/post, added token expiry checks, graceful 'Sign in to view' empty states in dashboard cards, allowed pre-compile navigation for Traces/Benchmarks/Builds (limiting preBuildTab to genuinely build-dependent tabs: Impact/Witnesses/Procedure/Patch/Tests/Approval), bumped asset cache-buster version to v=20260919-auth-init; verify 178/178 PASS, pytest 127/127 PASS, validate_infra PASS, node --check PASS. Claim released. | commit: 'T47: sequence frontend auth initialization, drop 401 alert loop, and unlock pre-compile tabs'

- 2026-09-20 02:30 | Buffy | T41d ROOT CAUSE CLOSED by evidence | AUTHDEBUG line captured the truth: groups_raw = "\"[pp-admins]\"" — API GW flattens the groups LIST to its bracket-form string and quotes it. T41b's comma-split produced {'[pp-admins]'} (bogus single group) → membership never matched → 403. FIX: _parse_groups() accepting every documented-observed shape (list / 'a,b' / '[a]' / '["a"]') with fail-closed garbage handling; verified against the EXACT CloudWatch bytes locally (role=admin, POST /builds authorized). pytest 100/100 (+1 live-shape test), verify PASS | commit: 'T41d: parse bracket-flattened cognito:groups (AUTHDEBUG-evidenced)' | NEXT: push → green → smoke attempt 6 should ride compile→SFN→gates→bundle→coverage; then REMOVE AUTHDEBUG (PP_AUTH_DEBUG=0 or delete block)- 2026-09-20 02:10 | Buffy | T41b stringified-groups fix | SMOKE ATTEMPT 4: login OK, GET /builds OK (3 builds), POST /builds STILL 403 with pp-admins token — T41 fallback didn't fire. REASONING: fallback only fires on EMPTY groups; if GW passes cognito:groups as a STRING ('pp-admins'), set('pp-admins') = 9 CHARACTERS — non-empty (no fallback) but membership fails (READ_ONLY, 403). Exactly the observed behavior. FIX: isinstance(str) → comma-split before set(); fallback retained for the truly-empty case. VERIFIED: string-shape → role=admin groups=[pp-admins]; comma-shape → both groups; pytest 99/99 (+1 test), verify PASS, authz parses clean | commit: 'T41b: handle stringified cognito:groups from authorizer contexts'- 2026-09-20 01:45 | Buffy | T41 group-claims fallback | SMOKE PROGRESS: login NOW WORKS (token issued, /builds 200 with 2 builds) → POST /builds 403 'writes require pp-reviewers or pp-admins' with token claims showing cognito:groups=[pp-admins]. Local reproduction with identical event shapes → AUTHORIZED (code is correct) → live delta = API GW strips colon-claims in authorizer context. FIX in authz.claims_from_event: group-less claims → re-derive via authenticate(headers) (stdlib RS256 + cached JWKS; Lambda-safe, no crypto dep) | catch (AuthzError, ValueError) for malformed tokens. VERIFIED: RSA sign→verify roundtrip through _rs256_verify with monkeypatched JWKS → role=admin via=cognito → POST /builds authorized; negative: garbage token → readonly (no 500), no header → readonly, groups-present → authorizer path. pytest 98/98 (+4), verify PASS, judge 6/6 | ALSO: smoke login switched to InitiateAuth (USER_PASSWORD_AUTH — /oauth2/token is code-flow-only), identity check via repo-local TMP (Windows curl can't see git-bash /tmp) | commit: 'T41: re-derive identity from bearer when GW drops cognito:groups'- 2026-09-20 01:20 | Buffy | T39 deploy green → T40 rotation bug | T39 landed green (scope fix + dedupe live). Smoke attempt 2: login → NotAuthorizedException 'Incorrect username or password' — ROOT CAUSE: T37's deploy ran deploy.sh whose demo-admin block ROTATED the password to a fresh random value on every deploy, silently clobbering the human-set one. FIX: deploy.sh never rotates (DEMO_PASSWORD env = explicit opt-in only). Local password re-set attempted but local creds (zayed@054728709828) are a DIFFERENT ACCOUNT from the stack (262786914860 per Cognito domain) — teammate must re-run admin-set-user-password (their shell has the pool ID in stack outputs) | DONE: bash -n OK, staged | commit: 'T40: deploy.sh never rotates demo password; DEMO_PASSWORD env opt-in'- 2026-09-20 01:00 | Buffy | T38 authorizer scope trap + smoke login fix | FIRST REAL CLOUD LOGIN ATTEMPT (user set demo password): (1) /oauth2/token password grant → unsupported_grant_type — endpoint serves only AllowedOAuthFlows [code]; moved to InitiateAuth USER_PASSWORD_AUTH (client has ALLOW_USER_PASSWORD_AUTH) + ID/access probe against /builds. (2) BOTH tokens 403 — authorizer AuthorizationScopes [openid,email] can never match: Cognito ID tokens carry no scope claim (access tokens carry them only for OAuth flows with resources). This would have 403'd the BROWSER flow too — the demo's first API call after login would die. Hidden until now because no account could log in. FIX: removed AuthorizationScopes (audience/issuer checks are the actual security boundary); scopes requirement was vestigial. deploy re-runs via CI on push; smoke re-run pending that | DONE: validate_infra PASS, pytest 94/94, staged | commit: 'T38: drop unsatisfiable authorizer scope requirement; smoke login via InitiateAuth'- 2026-09-20 00:40 | Buffy | T37 CI deploy exit-3 | ROOT CAUSE: Amplify app named after repo (Bharatbuilds), deploy.sh looked for processpatch-demo (stack suffix) — lookup returned None, T33's loud guard exited 3 BEFORE the second sam pass (first pass succeeded: stack updated, CORS allow-list + DemoAdmin live). FIX: discovery order FRONTEND_ORIGIN env → exact name → scan-all-apps/single-amplifyapp-domain fallback (python inline, ambiguous>1 → none) → loud exit; CI pins FRONTEND_ORIGIN=https://main.d1bq8os9dwj0d.amplifyapp.com (deterministic; discovery now local-run fallback only) | DONE: bash -n OK, validate_infra PASS, pytest 94/94, fallback logic unit-probed against the real domain | commit: 'T37: Amplify discovery by domain fallback + CI-pinned FRONTEND_ORIGIN'- 2026-09-20 00:20 | Buffy | T35 cloud E2E smoke + T33 live verification | POST-T33 LIVE AUDIT: API CORS now echoes access-control-allow-origin for https://main.d1bq8os9dwj0d.amplifyapp.com (deploy landed via CI on push); Cognito callback whitelist enforced (evil origin → redirect_mismatch, ours → login 302); frontend still byte-identical to local. NEW: scripts/cloud_smoke.sh <api> <email> <pass> — password grant → Bearer → POST /builds {defer:true} → /execute → poll to NEEDS_REVIEW → gate rule_review (decisions:{}) → poll NEEDS_APPROVAL → patch_approval APPROVE → activation APPROVE → governance-bundle (approvals count + sha) → coverage; exit 2=login, 3=stage-fail with named gate; LIVE-verified config+login paths (wrong-pass → clean 400→exit 2). KNOWN GAP (deliberate): full 3-gate run needs the real demo password — deploy.sh prints a fresh random one per deploy (CI-log visible on public repo); recommended: team sets a known password once via admin-set-user-password (command in docs/auth.md per T34) — no code change, no churn | DONE: bash -n OK, live probe OK | commit: 'T35: cloud E2E smoke script; T33 deploy verified live'- 2026-09-19 23:50 | Buffy | T33 live-deploy demo blockers | LIVE AUDIT: config.js now points at real API (served by amplify.yml), frontend md5-identical to local (fully current build), health 200, Cognito authorizer enforcing 401 on protected routes — BUT (1) API CORS echoed allow-origin only for https://main.dummy.amplifyapp.com (deploy.sh Amplify-app name lookup failed silently → bootstrap placeholder won) so every browser at the real URL is blocked; (2) OPTIONS preflights 401 (JWT authorizer eats preflight — benign since GET/POST are simple-request-exempt with Authorization header being CORS-safelisted, but HttpApi CorsConfiguration handles it once origins are right); (3) NO demo account exists anywhere (invite-only pool) — nobody could ever log in. FIXES: template CorsConfiguration hardcodes live URL + localhost:8080 alongside FrontendOrigin (stale allow-list entry costs nothing, missing live entry costs the demo); CallbackURLs + live URL + localhost:8080; DemoAdmin UserPoolUser (SUPPRESS, pp-admins attachment) + deploy.sh sets random password post-deploy, prints ONCE; deploy.sh discovery now FRONTEND_ORIGIN env > Amplify lookup > LOUD exit-3 (never silent-dummy again) | DONE: bash -n OK, validate_infra PASS, pytest 94/94, verify 178/178 | commit: 'T33: deploy-demo blockers — live-URL CORS, demo admin via IaC, loud FrontendOrigin discovery'- 2026-09-19 (T18) | Buffy | command palette + real coverage endpoint + Impact coverage panel | actions.py coverage(): node coverage joins LOCALIZER FAULTS by witness_id (witness dicts carry NO affected_nodes — first attempt joined on a field that doesn't exist and showed 0/2; corrected to the same localize_all call the impact engine uses; 2/2 with witness attribution W-001/NODE-ELIGIBILITY, W-002/NODE-REC-UPLOAD), field coverage = affected_rules (from build impact artifact) x witness case keys; note string keeps it honest ('covered = a verified witness exercises this node/field; nothing else counts') + routes server.py & aws_handlers.py (both surfaces) + UI: Impact panel with tone-coded bars (pass>=100%, warn>=50%, fail<50%) + uncovered list, fillCoverage() async after vImpact setView, skeleton while loading + palette: Ctrl/Cmd+K, 16 commands (10 tabs + compile/cloud/theme/judge/bundle/submit-policy), substring filter, role=dialog+listbox/option ARIA, Esc/overlay-click close, theme-token CSS, header command button | DONE: pytest 94/94 (+3 coverage tests), verify 178/178, judge_demo 6/6, journey 7/7, DOM-stub 'ALL RENDER PATHS + PALETTE OK (16 commands)', LIVE: coverage 2/2 nodes 1/1 fields via app runner | commit: 'T18: command palette (Ctrl+K), witness-coverage endpoint + Impact panel'

- 2026-09-19 | Codex | T43 pre-compile tab navigation | `frontend/lib/app.js` now renders honest per-tab empty states before a build exists instead of returning the Overview hero for every tab; versioned frontend asset URLs ensure the running app reloads this change. | DONE: Impact and Witnesses verified navigable before compile in localhost:8097; compile action still loads the full build-specific Overview; node syntax PASS, verify 178/178, pytest 118/118, diff check PASS. Claim released; no commit/push/deploy. | proposed commit: Make pre-compile tabs navigable with honest empty states

- 2026-09-19 | Codex | T42 remove Judge Mode | Removed header button, walkthrough dock/state/functions, palette entry, associated CSS and design documentation. Versioned app/style asset URLs so the running UI loads the removal despite cached assets. | DONE: node syntax PASS, verify 178/178, pytest 118/118; refreshed localhost:8097 and verified header and palette contain no Judge Mode. Claim released; no commit/push/deploy. | proposed commit: Remove Judge Mode and its walkthrough controls

- 2026-09-19 | Codex | T41 readiness audit and hardening | Claimed paths only: authz, storage, API actions/server/Lambda, infra template, frontend shell/styles/app, new readiness tests and report | DONE: 118 pytest passed, verify 178/178, infra PASS, node syntax PASS, diff whitespace PASS; browser canonical compile and ten tabs at 390px without page overflow, desktop editor and palette keyboard checks. Fixed PKCE/state, malformed tokens/JSON, model auto-accept override, DynamoDB pagination, atomic file replacement, stale/duplicate build state, swallowed build persistence failures, CORS/health routes, responsive UI and validation labels. Remaining production blockers recorded in docs/production-readiness.md (concurrency/migration, workspace isolation, inline handlers, staging/restore/load verification). Claims released. No commit/push/deploy. | proposed commit: Harden auth, persistence and request validation; improve responsive UI and document release blockers

- 2026-09-20 01:20 | Buffy | T39 deploy green → T40 rotation bug | T39 landed green (scope fix + dedupe live). Smoke attempt 2: login → NotAuthorizedException 'Incorrect username or password' — ROOT CAUSE: T37's deploy ran deploy.sh whose demo-admin block ROTATED the password to a fresh random value on every deploy, silently clobbering the human-set one. FIX: deploy.sh never rotates (DEMO_PASSWORD env = explicit opt-in only). Local password re-set attempted but local creds (zayed@054728709828) are a DIFFERENT ACCOUNT from the stack (262786914860 per Cognito domain) — teammate must re-run admin-set-user-password (their shell has the pool ID in stack outputs) | DONE: bash -n OK, staged | commit: 'T40: deploy.sh never rotates demo password; DEMO_PASSWORD env opt-in'- 2026-09-20 01:00 | Buffy | T38 authorizer scope trap + smoke login fix | FIRST REAL CLOUD LOGIN ATTEMPT (user set demo password): (1) /oauth2/token password grant → unsupported_grant_type — endpoint serves only AllowedOAuthFlows [code]; moved to InitiateAuth USER_PASSWORD_AUTH (client has ALLOW_USER_PASSWORD_AUTH) + ID/access probe against /builds. (2) BOTH tokens 403 — authorizer AuthorizationScopes [openid,email] can never match: Cognito ID tokens carry no scope claim (access tokens carry them only for OAuth flows with resources). This would have 403'd the BROWSER flow too — the demo's first API call after login would die. Hidden until now because no account could log in. FIX: removed AuthorizationScopes (audience/issuer checks are the actual security boundary); scopes requirement was vestigial. deploy re-runs via CI on push; smoke re-run pending that | DONE: validate_infra PASS, pytest 94/94, staged | commit: 'T38: drop unsatisfiable authorizer scope requirement; smoke login via InitiateAuth'- 2026-09-20 00:40 | Buffy | T37 CI deploy exit-3 | ROOT CAUSE: Amplify app named after repo (Bharatbuilds), deploy.sh looked for processpatch-demo (stack suffix) — lookup returned None, T33's loud guard exited 3 BEFORE the second sam pass (first pass succeeded: stack updated, CORS allow-list + DemoAdmin live). FIX: discovery order FRONTEND_ORIGIN env → exact name → scan-all-apps/single-amplifyapp-domain fallback (python inline, ambiguous>1 → none) → loud exit; CI pins FRONTEND_ORIGIN=https://main.d1bq8os9dwj0d.amplifyapp.com (deterministic; discovery now local-run fallback only) | DONE: bash -n OK, validate_infra PASS, pytest 94/94, fallback logic unit-probed against the real domain | commit: 'T37: Amplify discovery by domain fallback + CI-pinned FRONTEND_ORIGIN'- 2026-09-20 00:20 | Buffy | T35 cloud E2E smoke + T33 live verification | POST-T33 LIVE AUDIT: API CORS now echoes access-control-allow-origin for https://main.d1bq8os9dwj0d.amplifyapp.com (deploy landed via CI on push); Cognito callback whitelist enforced (evil origin → redirect_mismatch, ours → login 302); frontend still byte-identical to local. NEW: scripts/cloud_smoke.sh <api> <email> <pass> — password grant → Bearer → POST /builds {defer:true} → /execute → poll to NEEDS_REVIEW → gate rule_review (decisions:{}) → poll NEEDS_APPROVAL → patch_approval APPROVE → activation APPROVE → governance-bundle (approvals count + sha) → coverage; exit 2=login, 3=stage-fail with named gate; LIVE-verified config+login paths (wrong-pass → clean 400→exit 2). KNOWN GAP (deliberate): full 3-gate run needs the real demo password — deploy.sh prints a fresh random one per deploy (CI-log visible on public repo); recommended: team sets a known password once via admin-set-user-password (command in docs/auth.md per T34) — no code change, no churn | DONE: bash -n OK, live probe OK | commit: 'T35: cloud E2E smoke script; T33 deploy verified live'- 2026-09-19 23:50 | Buffy | T33 live-deploy demo blockers | LIVE AUDIT: config.js now points at real API (served by amplify.yml), frontend md5-identical to local (fully current build), health 200, Cognito authorizer enforcing 401 on protected routes — BUT (1) API CORS echoed allow-origin only for https://main.dummy.amplifyapp.com (deploy.sh Amplify-app name lookup failed silently → bootstrap placeholder won) so every browser at the real URL is blocked; (2) OPTIONS preflights 401 (JWT authorizer eats preflight — benign since GET/POST are simple-request-exempt with Authorization header being CORS-safelisted, but HttpApi CorsConfiguration handles it once origins are right); (3) NO demo account exists anywhere (invite-only pool) — nobody could ever log in. FIXES: template CorsConfiguration hardcodes live URL + localhost:8080 alongside FrontendOrigin (stale allow-list entry costs nothing, missing live entry costs the demo); CallbackURLs + live URL + localhost:8080; DemoAdmin UserPoolUser (SUPPRESS, pp-admins attachment) + deploy.sh sets random password post-deploy, prints ONCE; deploy.sh discovery now FRONTEND_ORIGIN env > Amplify lookup > LOUD exit-3 (never silent-dummy again) | DONE: bash -n OK, validate_infra PASS, pytest 94/94, verify 178/178 | commit: 'T33: deploy-demo blockers — live-URL CORS, demo admin via IaC, loud FrontendOrigin discovery'- 2026-09-19 (T18) | Buffy | command palette + real coverage endpoint + Impact coverage panel | actions.py coverage(): node coverage joins LOCALIZER FAULTS by witness_id (witness dicts carry NO affected_nodes — first attempt joined on a field that doesn't exist and showed 0/2; corrected to the same localize_all call the impact engine uses; 2/2 with witness attribution W-001/NODE-ELIGIBILITY, W-002/NODE-REC-UPLOAD), field coverage = affected_rules (from build impact artifact) x witness case keys; note string keeps it honest ('covered = a verified witness exercises this node/field; nothing else counts') + routes server.py & aws_handlers.py (both surfaces) + UI: Impact panel with tone-coded bars (pass>=100%, warn>=50%, fail<50%) + uncovered list, fillCoverage() async after vImpact setView, skeleton while loading + palette: Ctrl/Cmd+K, 16 commands (10 tabs + compile/cloud/theme/judge/bundle/submit-policy), substring filter, role=dialog+listbox/option ARIA, Esc/overlay-click close, theme-token CSS, header command button | DONE: pytest 94/94 (+3 coverage tests), verify 178/178, judge_demo 6/6, journey 7/7, DOM-stub 'ALL RENDER PATHS + PALETTE OK (16 commands)', LIVE: coverage 2/2 nodes 1/1 fields via app runner | commit: 'T18: command palette (Ctrl+K), witness-coverage endpoint + Impact panel'
- 2026-09-20 02:10 | Buffy | T41b stringified-groups fix | SMOKE ATTEMPT 4: login OK, GET /builds OK (3 builds), POST /builds STILL 403 with pp-admins token — T41 fallback didn't fire. REASONING: fallback only fires on EMPTY groups; if GW passes cognito:groups as a STRING ('pp-admins'), set('pp-admins') = 9 CHARACTERS — non-empty (no fallback) but membership fails (READ_ONLY, 403). Exactly the observed behavior. FIX: isinstance(str) → comma-split before set(); fallback retained for the truly-empty case. VERIFIED: string-shape → role=admin groups=[pp-admins]; comma-shape → both groups; pytest 99/99 (+1 test), verify PASS, authz parses clean | commit: 'T41b: handle stringified cognito:groups from authorizer contexts'- 2026-09-20 01:45 | Buffy | T41 group-claims fallback | SMOKE PROGRESS: login NOW WORKS (token issued, /builds 200 with 2 builds) → POST /builds 403 'writes require pp-reviewers or pp-admins' with token claims showing cognito:groups=[pp-admins]. Local reproduction with identical event shapes → AUTHORIZED (code is correct) → live delta = API GW strips colon-claims in authorizer context. FIX in authz.claims_from_event: group-less claims → re-derive via authenticate(headers) (stdlib RS256 + cached JWKS; Lambda-safe, no crypto dep) | catch (AuthzError, ValueError) for malformed tokens. VERIFIED: RSA sign→verify roundtrip through _rs256_verify with monkeypatched JWKS → role=admin via=cognito → POST /builds authorized; negative: garbage token → readonly (no 500), no header → readonly, groups-present → authorizer path. pytest 98/98 (+4), verify PASS, judge 6/6 | ALSO: smoke login switched to InitiateAuth (USER_PASSWORD_AUTH — /oauth2/token is code-flow-only), identity check via repo-local TMP (Windows curl can't see git-bash /tmp) | commit: 'T41: re-derive identity from bearer when GW drops cognito:groups'- 2026-09-20 01:20 | Buffy | T39 deploy green → T40 rotation bug | T39 landed green (scope fix + dedupe live). Smoke attempt 2: login → NotAuthorizedException 'Incorrect username or password' — ROOT CAUSE: T37's deploy ran deploy.sh whose demo-admin block ROTATED the password to a fresh random value on every deploy, silently clobbering the human-set one. FIX: deploy.sh never rotates (DEMO_PASSWORD env = explicit opt-in only). Local password re-set attempted but local creds (zayed@054728709828) are a DIFFERENT ACCOUNT from the stack (262786914860 per Cognito domain) — teammate must re-run admin-set-user-password (their shell has the pool ID in stack outputs) | DONE: bash -n OK, staged | commit: 'T40: deploy.sh never rotates demo password; DEMO_PASSWORD env opt-in'- 2026-09-20 01:00 | Buffy | T38 authorizer scope trap + smoke login fix | FIRST REAL CLOUD LOGIN ATTEMPT (user set demo password): (1) /oauth2/token password grant → unsupported_grant_type — endpoint serves only AllowedOAuthFlows [code]; moved to InitiateAuth USER_PASSWORD_AUTH (client has ALLOW_USER_PASSWORD_AUTH) + ID/access probe against /builds. (2) BOTH tokens 403 — authorizer AuthorizationScopes [openid,email] can never match: Cognito ID tokens carry no scope claim (access tokens carry them only for OAuth flows with resources). This would have 403'd the BROWSER flow too — the demo's first API call after login would die. Hidden until now because no account could log in. FIX: removed AuthorizationScopes (audience/issuer checks are the actual security boundary); scopes requirement was vestigial. deploy re-runs via CI on push; smoke re-run pending that | DONE: validate_infra PASS, pytest 94/94, staged | commit: 'T38: drop unsatisfiable authorizer scope requirement; smoke login via InitiateAuth'- 2026-09-20 00:40 | Buffy | T37 CI deploy exit-3 | ROOT CAUSE: Amplify app named after repo (Bharatbuilds), deploy.sh looked for processpatch-demo (stack suffix) — lookup returned None, T33's loud guard exited 3 BEFORE the second sam pass (first pass succeeded: stack updated, CORS allow-list + DemoAdmin live). FIX: discovery order FRONTEND_ORIGIN env → exact name → scan-all-apps/single-amplifyapp-domain fallback (python inline, ambiguous>1 → none) → loud exit; CI pins FRONTEND_ORIGIN=https://main.d1bq8os9dwj0d.amplifyapp.com (deterministic; discovery now local-run fallback only) | DONE: bash -n OK, validate_infra PASS, pytest 94/94, fallback logic unit-probed against the real domain | commit: 'T37: Amplify discovery by domain fallback + CI-pinned FRONTEND_ORIGIN'- 2026-09-20 00:20 | Buffy | T35 cloud E2E smoke + T33 live verification | POST-T33 LIVE AUDIT: API CORS now echoes access-control-allow-origin for https://main.d1bq8os9dwj0d.amplifyapp.com (deploy landed via CI on push); Cognito callback whitelist enforced (evil origin → redirect_mismatch, ours → login 302); frontend still byte-identical to local. NEW: scripts/cloud_smoke.sh <api> <email> <pass> — password grant → Bearer → POST /builds {defer:true} → /execute → poll to NEEDS_REVIEW → gate rule_review (decisions:{}) → poll NEEDS_APPROVAL → patch_approval APPROVE → activation APPROVE → governance-bundle (approvals count + sha) → coverage; exit 2=login, 3=stage-fail with named gate; LIVE-verified config+login paths (wrong-pass → clean 400→exit 2). KNOWN GAP (deliberate): full 3-gate run needs the real demo password — deploy.sh prints a fresh random one per deploy (CI-log visible on public repo); recommended: team sets a known password once via admin-set-user-password (command in docs/auth.md per T34) — no code change, no churn | DONE: bash -n OK, live probe OK | commit: 'T35: cloud E2E smoke script; T33 deploy verified live'- 2026-09-19 23:50 | Buffy | T33 live-deploy demo blockers | LIVE AUDIT: config.js now points at real API (served by amplify.yml), frontend md5-identical to local (fully current build), health 200, Cognito authorizer enforcing 401 on protected routes — BUT (1) API CORS echoed allow-origin only for https://main.dummy.amplifyapp.com (deploy.sh Amplify-app name lookup failed silently → bootstrap placeholder won) so every browser at the real URL is blocked; (2) OPTIONS preflights 401 (JWT authorizer eats preflight — benign since GET/POST are simple-request-exempt with Authorization header being CORS-safelisted, but HttpApi CorsConfiguration handles it once origins are right); (3) NO demo account exists anywhere (invite-only pool) — nobody could ever log in. FIXES: template CorsConfiguration hardcodes live URL + localhost:8080 alongside FrontendOrigin (stale allow-list entry costs nothing, missing live entry costs the demo); CallbackURLs + live URL + localhost:8080; DemoAdmin UserPoolUser (SUPPRESS, pp-admins attachment) + deploy.sh sets random password post-deploy, prints ONCE; deploy.sh discovery now FRONTEND_ORIGIN env > Amplify lookup > LOUD exit-3 (never silent-dummy again) | DONE: bash -n OK, validate_infra PASS, pytest 94/94, verify 178/178 | commit: 'T33: deploy-demo blockers — live-URL CORS, demo admin via IaC, loud FrontendOrigin discovery'- 2026-09-19 (T18) | Buffy | command palette + real coverage endpoint + Impact coverage panel | actions.py coverage(): node coverage joins LOCALIZER FAULTS by witness_id (witness dicts carry NO affected_nodes — first attempt joined on a field that doesn't exist and showed 0/2; corrected to the same localize_all call the impact engine uses; 2/2 with witness attribution W-001/NODE-ELIGIBILITY, W-002/NODE-REC-UPLOAD), field coverage = affected_rules (from build impact artifact) x witness case keys; note string keeps it honest ('covered = a verified witness exercises this node/field; nothing else counts') + routes server.py & aws_handlers.py (both surfaces) + UI: Impact panel with tone-coded bars (pass>=100%, warn>=50%, fail<50%) + uncovered list, fillCoverage() async after vImpact setView, skeleton while loading + palette: Ctrl/Cmd+K, 16 commands (10 tabs + compile/cloud/theme/judge/bundle/submit-policy), substring filter, role=dialog+listbox/option ARIA, Esc/overlay-click close, theme-token CSS, header command button | DONE: pytest 94/94 (+3 coverage tests), verify 178/178, judge_demo 6/6, journey 7/7, DOM-stub 'ALL RENDER PATHS + PALETTE OK (16 commands)', LIVE: coverage 2/2 nodes 1/1 fields via app runner | commit: 'T18: command palette (Ctrl+K), witness-coverage endpoint + Impact panel'
- 2026-09-20 08:30 | Buffy | T57 env flags were truthiness-tested (the string '0' ENABLED them) | Found while making the purge flag switchable: `if os.environ.get("PP_AUTH_DEBUG")` is TRUE for the string '0' — the AUTHDEBUG claim dump would have stayed on in production no matter what the flag said (the RESUMEDEBUG branch used `== "1"`, so the two halves of the same flag disagreed). FIX: `aws_handlers._flag(name)` parses 1/true/yes/on only, used by both branches; proof run: '0'→False, '1'→True, unset→False. TEMPLATE: PP_AUTH_DEBUG is no longer pinned '1' (the auth loop is closed — T41d/T50 hold live), and both flags are now CloudFormation parameters (`AuthDebug`, `DemoPurge`, AllowedValues 0/1, default 0) so the teammate can enable a demo purge at deploy time without a code change. The DemoPurge description states the boundary explicitly: /archive is how builds are retired; /purge exists to reset a demo stack and logs claim/token data stays off in front of an audience. infra 33/33 PASS, pytest 151/151 | commit: 'T57: parse env flags properly; AuthDebug/DemoPurge as deploy parameters'
- 2026-09-20 08:15 | Buffy | T56 live demo driver — proven on the deployed stack, twice | `scripts/demo_run.py <api> <email> <pass>`: rewrites the canonical policy's CGPA threshold from a candidate list until POST /builds reports NO idempotent reuse (the canonical policy is already compiled, so a naive demo click returns the cached build and never reaches a gate), then rides compile → adopt the SFN-persisted BUILD- → Gate 1 only if extraction parks at NEEDS_REVIEW → Gate 2 (APPROVE_CANDIDATE, value read from the ASL vocabulary) → Gate 3 (APPROVE) → PATCH_ACTIVE → governance bundle + witness coverage + execution status. Shares auth helpers with demo_cleanup (single login implementation). LIVE PROOF: BUILD-C11F029F8A9B — CGPA 6.95 amendment → p8/APR-20260920-0008/candidate WF-V3-PATCH-246F4AAF → activation → PATCH_ACTIVE, bundle 2 approvals sha f2038b3e630a77fe, coverage nodes 2/2 fields 1/1, execution SUCCEEDED. Second fully-activated cloud build, and this one was produced by the rehearsal script itself | commit: 'T56: demo_run.py — fresh amendment through all three gates, live-verified'
- 2026-09-20 07:55 | Buffy | T55 flagged hard-purge route (purge was requested alongside archival) | Archival stays the supported way to retire a build; the one true delete path is now explicit and defended at four levels: (1) REFUSED unless PP_DEMO_PURGE is set on the stack (destruction is never one env var away from normal behaviour), (2) admin-only via authz._needs_admin (`/purge`), (3) audited BEFORE anything is removed — BUILD_PURGED with actor/status/full — so the act stays on record even though the evidence does not, (4) scoped: `purge_build_records` sweeps callbacks/rule_reviews/patch_reviews/approvals/candidates by build_id and deliberately NEVER touches procedure_versions.json (the shared registry, not per-build evidence). `{"full": false}` deletes only the document. Both surfaces wired (POST /builds/{id}/purge; PermissionError→403 added to aws call() and the local server route); `demo_cleanup.py --purge` switches the tool from hide to destroy and says so. TESTS: `test_build_purge.py` (6: refused without the flag, removes build + governance rows, audited with actor, partial purge keeps rows, unknown id → KeyError, authz shape for archive/unarchive/purge). pytest 151/151, infra 33/33 | commit: 'T55: PP_DEMO_PURGE hard-delete route, admin-only and audited'
- 2026-09-20 07:40 | Buffy | T54 build archival — clean the console without destroying evidence | STACK AUDIT: 13 build docs = 1 activated (BUILD-78BA53EC79BF PATCH_ACTIVE) + 7 parked PATCH_VALIDATED (gate-war residue) + 5 DRAFT-*. Chose ARCHIVAL over deletion: this is a governance product, evidence is never destroyed; every build stays readable by id, resumable, and the audit log records BUILD_ARCHIVED. IMPL: `registry.store.set_build_archived(bid, archived)` (+`list_builds(include_archived=False)`), `actions.archive_build/unarchive_build` (KeyError→404 via _need), routes on BOTH surfaces (POST /builds/{id}/archive|unarchive; GET /builds?include_archived=1 — server.py gained a `_truthy` flag parser, aws_handlers the same flag), and `scripts/demo_cleanup.py` (dry-run default, --apply, --keep) which keeps PATCH_ACTIVE/ACTIVE and archives the rest. DOCUMENTED SEMANTICS: idempotency is deliberately UNAFFECTED (find_build_by_key still matches archived builds — archiving hides a build from the working list, it does not un-happen the compile); the same semantic means a repeat demo of the unchanged canonical policy shows a cache hit, not a fresh compile — flagged for the demo script. TESTS: `tests/unit/test_build_archive.py` (5: leaves default listing, reversible+audited, evidence still readable, idempotency unaffected, unknown id → KeyError) + Codex's `test_readiness.py` outage stub widened to `(*_args, **_kwargs)` (my route now forwards the include_archived flag; the stub was 0-arg so the injected RuntimeError became a TypeError → 500 instead of the asserted 503). pytest **145/145**, verify **178/178**, infra **33/33**. LIVE DRY RUN: 13 builds → 1 keep, 12 archive (apply pending deploy) | commit: 'T54: archival (hide, never delete) for parked builds + demo cleanup script'
- 2026-09-20 07:10 | Buffy | T53 ASL gate contracts — all three gates, derived from the state machine | The T51 post-mortem left one open question: how many OTHER payload/ASL mismatches were waiting a deploy cycle each? Built the class test. `tests/unit/test_asl_gate_contracts.py` derives, from `infra/statemachine.asl.json` itself: (1) which waitForTaskToken states exist and which gate each is (from its Lambda payload op/kind), (2) every `$.<ResultPath>.<field>` the document reads ANYWHERE (deep walk), (3) the approve value the ASL's Choice expects for each gate — then drives the REAL local gate chain (canonical compile → rule_review → patch_approval → activation) and asserts each payload satisfies its references; the final test asserts the union of payloads covers every reference in the document (a new ASL read with no payload producer fails here). MUTATION-VERIFIED: changed ACTIVATE's `reviewer.$` to `$.activation_decision.approver` and exactly the two relevant tests failed with `ASL reads fields no gate payload produces: {'activation': {'approver'}}`; ASL restored, `git diff` clean. ANSWER TO THE OPEN QUESTION: zero other mismatches — rule_review (accepted/accepted_rules) and patch_approval (decision) were already correct. pytest **140/140** at that point | commit: 'T53: ASL-derived contract tests for all three human gates'
- 2026-09-20 06:45 | Buffy | T52 ✅ FULL 3-GATE CLOUD E2E GREEN — first activated cloud build in product history | ATTEMPT 16 on the T51 deploy, fresh policy CGPA 6.85 → DRAFT-C9476201 → **BUILD-78BA53EC79BF**: compiled + persisted → `PATCH_VALIDATED` (clean extraction, Gate 1 auto-passed) → **Gate 2 HTTP 200** ({decision: APPROVE_CANDIDATE, approval_id: APR-20260920-0006, candidate_version_id: WF-V3-PATCH-39A33A49}) → **Gate 3 HTTP 200** ({decision: APPROVE, reviewer: demo-admin@processpatch.demo, reason: cloud-e2e} — the T51 payload) → ACTIVATE → MARK_ACTIVE → **`PATCH_ACTIVE` in 3 seconds** → SFN execution **`SUCCEEDED`, error: null** → governance bundle **2 approvals** (patch + activation) sha `4b6ceb12ae250eb2` → coverage **nodes 2/2, fields 1/1** with witness attribution (W-001/NODE-ELIGIBILITY, W-002/NODE-REC-UPLOAD). NO code change required for this run — it validates T41d→T46→T48→T50→T51 end to end. THE CHAIN THAT GOT HERE (all live-evidenced, not hypothesized): authorizer scope trap (T38) → group claim shapes (T41b/T41d, AUTHDEBUG bytes) → draft-poisons-its-own-idempotency (T45) → jsonschema missing from the Lambda bundle (T46) → DescribeExecution IAM resource shape + output-only-at-terminal discovery (T47) → send_task_success kwarg (T48, named by its own response body) → describe_execution error/cause surfacing (T49, which named T50 in ONE probe) → DynamoDB duplicate-SK on gate callbacks (T50) → activation payload vs ACTIVATE ASL contract (T51). NEXT: demo rehearsal against the live stack (scripts/cloud_smoke.sh now proven end-to-end by attempt 16's exact flow); optional cleanup of the four parked half-approved builds (856A/F75E/D4AF + earlier) if the demo dataset should show only completed runs | no commit: log entry only
- 2026-09-20 06:15 | Buffy | T51 ATTEMPT 15 REACHED ACTIVATE — and died on the LAST contract gap | T50 WORKED: fresh policy CGPA 6.95 → BUILD-D4AF7EB08CF9 compiled, Gate 2 answered **HTTP 200 clean** (APR-20260920-0005, candidate WF-V3-PATCH-08DCDD72 — no more duplicate-SK 500), **Gate 3 ARMED and processed** (HTTP 200 `{"decision":"APPROVE"}`), FETCH_CANDIDATE found its candidate, ACTIVATION_APPROVED? matched. Then the execution died ONE STATE LATER, and T49's cause was exact: `States.Runtime: state 'ACTIVATE' … JSONPath '$.activation_decision.reviewer' specified for the field 'reviewer.$' could not be found`. ROOT CAUSE: the wait state's ResultPath IS the task output, and `resume_callback`'s activation branch emitted only `{"decision":"APPROVE"}` while the ASL's ACTIVATE Parameters read `$.activation_decision.reviewer` + `.reason` (line 165) → every Gate-3 approval in product history would have died at ACTIVATE with the approval half-applied. FULL ASL SWEEP: this was the ONLY human-gate payload mismatch (rule_review_wait → accepted/accepted_rules ✓ emitted; patch_decision.decision ✓; activation_decision.decision ✓) — one contract gap, not a class. FIX: activation branch now emits `{decision, reviewer, reason}` (the reviewer dict is what activate_procedure stamps into the FINAL_APPROVER record), and a decision without `reviewer.reviewer_id` now raises ValueError **AT THE GATE** (400 to the human) instead of signalling an execution into a guaranteed States.Runtime. TESTS: new `tests/unit/test_t51_activation_payload.py` (4) incl. a CONTRACT TEST that derives the required keys from `infra/statemachine.asl.json` itself (`WAIT_FOR_ACTIVATION_APPROVAL.ResultPath` prefix → ACTIVATE Parameters) and asserts the gate output supplies them — drift-proof. pytest **135/135** | PENDING: human commit → push → deploy → attempt 16 (fresh threshold 6.85) = FULL 3-GATE CLOUD E2E: compile → Gate 2 → Gate 3 → ACTIVATE → PATCH_ACTIVE → bundle + coverage (bundle/coverage already verified live on D4AF: 1 approval, sha 49c33aeccdfc969b, nodes 2/2, fields 1/1) | commit: 'T51: activation gate emits the reviewer/reason ACTIVATE reads'
- 2026-09-20 05:40 | Buffy | T50 THE WALL BETWEEN GATES 2 AND 3: DynamoDB duplicate item keys in callbacks.json | ATTEMPT 14 (fresh novel policy CGPA 7.05 → DRAFT-C1263D1F, ARN captured from /execute) rode compile → BUILD-F75EC0BCC157 PATCH_VALIDATED → Gate-2 resume came back **500 AFTER the decision committed** (review_state=REVIEWED, approval+candidate minted) → state machine proceeded past `PATCH_APPROVED?` → FETCH_CANDIDATE **found the candidate** → died ARMING Gate 3. T49's error/cause named it in one shot, no CloudWatch needed: `RuntimeError: storage save failed for callbacks.json: ClientError ValidationException (BatchWriteItem): Provided list of item keys contains duplicates` at `save_callback` (governance/store.py) via govern_handler:183. ROOT CAUSE in `services/storage.py`: `_item_id()` derived the DynamoDB SK from `review_id→approval_id→policy_version_id→procedure_version_id→executionArn→build_id`; callback records ({build_id,gate,task_token,...}) have no dedicated id, so EVERY gate of a build derived the same SK (`build_id`). The moment a build arms a second gate — or the collection already carries a legacy duplicate pair from the APR-0002 probe + re-fire — the new list contains two items with one SK, `batch_writer` puts both, BatchWriteItem rejects the batch, and `_save` raises → the wait state's Lambda invocation fails → execution FAILED. Same bug produced the Gate-2 500 (trailing save). FIX: (1) `_item_id` — dedicated ids win first, then composite `f"{build_id}#{gate}"` for gate records, then bare build_id; (2) new pure `_sks(name,obj)` collapses duplicate SKs last-wins (append semantics, matching save_callback's in-list replace) so an already-poisoned production collection SELF-HEALS on its next save instead of wedging forever; `_dd_save` uses it; (3) `describe_execution` maps `ExecutionDoesNotExist` → KeyError → 404 (a fabricated ARN was 500ing, hiding unknown-execution from the API surface). PROOF: new `tests/unit/test_t50_callback_keys.py` — first run FAILED and caught a real ordering bug in my own fix (composite branch swallowed review records that carry both `review_id` and `gate`) → reordered; pytest **131/131**; repro on a poisoned collection (two same-build/same-gate records + gate-3 arming) → distinct SKs `BUILD-F75EC0BCC157#patch_approval` / `#activation`, `get_callback(activation)` found, arming OK | PENDING: human commit → push → CI deploy → attempt 15 = full 3-gate cloud E2E (compile → Gate 2 APPROVE_CANDIDATE → Gate 3 → PATCH_ACTIVE → bundle+coverage) | commit: 'T50: key gate callbacks by (build_id, gate) in the DynamoDB adapter'
- 2026-09-20 05:10 | Buffy | T49 failed executions name their own cause | Gate-2 re-fire post-T48 came back CLEAN (`{decision: APPROVE_CANDIDATE, approval_id: APR-20260919-0003, candidate_version_id: WF-V3-PATCH-68FCA69A}`, no resume_error → the SFN signal landed) but the execution then died before Gate 3 armed; `GET /executions/{arn}` finally worked (T47 IAM) and read `FAILED` — while boto3's `describe_execution` carries `error`+`cause`, our wrapper was dropping both, making failed executions undebuggable from the API. FIX: wrapper returns `error`, `cause` (1500c), `output` (2000c) | DONE: staged, pytest unit 99/99 | commit: 'T49: describe_execution surfaces error/cause for failed executions' — this is the line that killed T50 in one probe
- 2026-09-20 04:45 | Buffy | T48 THE BUG: send_task_success(taskOutput=) — boto3 wants output | T47 evidence probe on parked BUILD-856A874DC343 paid off IMMEDIATELY — and the response body itself named the bug (no CloudWatch needed): Gate 2 resume → HTTP 200 {decision: APPROVE_CANDIDATE, approval_id: APR-20260919-0002, candidate_version_id: WF-V3-PATCH-68FCA69A} — the ENTIRE decide chain works in the cloud; then resume_error: ParamValidationError — send_task_success called with taskOutput=... but boto3's kwarg is output=. So EVERY prior attempt's resume never signalled SFN (earlier 404s never even reached the call; this was its first live execution). resume_callback keeps the DECIDED_LOCALLY record WITH the original task token → after deploy, re-firing the same Gate-2 resume re-signals the STILL-WAITING task → execution un-waits → FETCH_CANDIDATE → Gate 3 arms → drive activation → PATCH_ACTIVE. Minor side effect: one duplicate APR record from the re-fire (demo dataset, acceptable) | FIX: one word (taskOutput→output) in governance/store.py:349 | PENDING: push → deploy → re-fire Gate 2 → Gate 3 → ACTIVE → bundle+coverage = FULL CLOUD E2E | commit: 'T48: send_task_success kwarg fix (output, not taskOutput)'
- 2026-09-20 04:20 | Buffy | T47 first REAL compile persisted; Gate-2 500 = last domino, evidence round | ATTEMPT 12-13 FORENSICS: T46 worked — attempt-13 build BUILD-856A874DC343 compiled+persisted live (FIRST real cloud compile in product history; 12/9084B5E7F601 was the jsonschema-deploy proof). Smoke still exit-3 because /executions/{arn} poll: (a) my T46 IAM resource shape was WRONG — ${BuildStateMachine}:* expands to :stateMachine: but executions live under :execution: → DescribeExecution still AccessDenied → 500; FIXED with exact execution-ARN statement (arn:...:execution:processpatch-build-${EnvName}:*). (b) DESIGN FLUSHED: SFN only exposes execution output at TERMINAL state, but the happy path NEVER terminates until gates are driven — output-based adoption is architecturally impossible; smoke now BASELINE-DIFFS /builds before/after execute and adopts the first new BUILD- entry. Gate-2 probe on 856A874DC343: NOT 'no waiting callback' — HTTP 500 = callback IS armed, resume_callback crashed in the decide chain (unmapped exception type → call() falls through to API-GW 500). LOCAL REPRO of the full chain (run_build + request_patch_review + save_callback + resume_callback APPROVE_CANDIDATE): PASSES — approval minted, candidate WF-V3-PATCH-0D59B76F, only resume_error boto3-missing (local, expected). Code correct → 500 is cloud-specific (DynamoDB-path or packaging). Guess budget spent → EVIDENCE ROUND: call() catch-all → CloudWatch traceback + RESUMEDEBUG line (PP_AUTH_DEBUG-gated, same flag as AUTHDEBUG) | PENDING: push → deploy → 1 probe POST on the next parked build → CloudWatch names the exception → one-line fix → full E2E | commit: 'T47: execution-IAM fix + baseline-diff build adoption + 500 evidence path'
- 2026-09-20 03:45 | Buffy | T46 missing Lambda dep (jsonschema) — the real attempt-11 killer | SMOKE ATTEMPT 11: T45 fix live, novel policy accepted (idempotent_reuse=False), execution started… and NO build ever persisted. Traps ruled out on the way: /executions/{arn} URL-encoding fine (raw probe = HTTP 500), proxy+ routing fine, IAM confirmed (ApiFn already has SendTaskSuccess '*'; DescribeExecution WAS missing though — added), deterministic build-id prediction (BUILD-906914208342 absent from registry) → execution FAILED or CLOSED_REVIEW pre-persist. ROOT CAUSE found by running the exact smoke policy through the local pipeline: services/schemas.check imports jsonschema (function-level, indented — first import sweep missed it; second sweep: boto3/traceback/jsonschema are the ONLY third-party names) but infra/lambda-requirements.txt shipped ONLY boto3. EXTRACT_RULES had NEVER actually executed in the cloud (every earlier run short-circuited READY_CACHED at idempotency) → ModuleNotFoundError → States.ALL retry x2 → execution FAILED. The novel-policy fix didn't break anything — it EXPOSED the missing dep. FIX: jsonschema>=4 in lambda-requirements.txt (venv 4.25.0 proven compatible, pytest green); +states:DescribeExecution for ApiFn (StartExecution + execution-resource statement). LOCAL PREDICTION for the 7.25 policy: EXTRACTED/schema-valid → PATCH_VALIDATED, 2 witnesses, 3 patch ops — parks exactly at Gate 2 | PENDING: push → CI deploy → smoke attempt 12 (same 7.25 policy — T45 un-poisons, T46 lets it compile) | commit: 'T46: ship jsonschema in the Lambda bundle; DescribeExecution for the API'
- 2026-09-20 03:10 | Buffy | T45 DRAFT-poisoned idempotency + smoke rides the real lifecycle | SMOKE ATTEMPTS 7-10 full forensics: (7) auth+write gate FINALLY cleared (T41d live), compile OK, SFN RUNNING — but parked at PATCH_VALIDATED, smoke polled for NEEDS_REVIEW which clean auto-accept never produces; (8-9) resume 404 'no waiting callback' → ROOT CAUSE NOT auth: POST /builds deferred DRAFT stores compile_key in the registry → SFN CHECK_IDEMPOTENT find_by_key() cache-hits the DRAFT → READY_CACHED → pipeline NEVER reaches WAIT_FOR_PATCH_APPROVAL → no taskToken ever saved → all resumes 404. The product's own idempotency ate the pipeline. Demo-killer: compile-same-policy-twice = second compile never produces gates. FIX (two surfaces): aws_handlers.find_by_key + actions.py POST /builds skip DRAFT-* hits (drafts are registration stubs, not compiled builds); 3 regression tests (pytest 127/127). SMOKE fixes: Gate 2 decision is APPROVE_CANDIDATE per ASL PATCH_APPROVED? (my APPROVE would 400 — decide_patch validates the same vocab); resume_gate() retries 'no waiting callback' for the SFN arm race; smoke submits a NOVEL amendment (CGPA 7.25) so canonical-build idempotency can't short-circuit; polls GET /executions/{arn-urlencoded} and adopts the hash-derived BUILD- id from execution output (DRAFT docs never mutate — attempt 9 polled DRAFT for 40 polls); explicit die-guard on idempotent_reuse=True; final PATCH_ACTIVE poll; attempt 9 exposed my own smoke draft (DRAFT-6AD0B59B) — now harmless after T45 deploy | PENDING: push → CI deploy → smoke attempt 11 with the same 7.25 policy (T45 un-poisons its draft) | commit: 'T45: drafts never satisfy idempotent reuse; smoke rides the real cloud lifecycle'
- 2026-09-20 02:40 | Buffy | T44 merge reconciliation (Codex T41–T43 into T41d line) | MERGE of 0fc9459 into T41d local line: Codex independently hit the same live 403 and shipped their own _groups parser + readiness hardening — BUT their branch carried COMMITTED conflict markers (their own 'Fix tab error' attempts). Resolved: authz.py keeps Codex's hardened authenticate (stricter exp/finite check, token_use validation, segment-count guard, binascii.Error→401) + their _groups (JSON-quoted + bracket-strip, fail-closed) + my T41 group-less bearer fallback; TEST FIX: garbage-groups case asserted readonly while a VALID signed token was still in headers — fallback correctly rescues; now asserts rescue-with-token AND fail-closed-without-header. agents.md flattened (all sides' logs kept). Also folded: Codex T42 (Judge Mode removed), T43 readiness work (docs/production-readiness.md, test_readiness.py, storage/actions/server/aws_handlers hardening, responsive UI pass) | DONE: pytest 124/124, verify 178/178, infra 33/33, LIVE-SHAPE replay (claims verbatim from CloudWatch AUTHDEBUG) → role=admin, POST /builds AUTHORIZED | commit: 'T44: merge Codex T41–T43; resolve authz parser + markers; fix fallback test'
- 2026-09-19 (T16b) | Buffy | lab theme promoted to DEFAULT (user call: rust identity for the demo) | frontend/lib/app.js boot default 'lab' (OS-preference detection removed — identity now deliberate, not ambient), theme cycle lab→paper→graphite→lab; frontend/index.html early-paint hint sets data-theme=lab before stylesheets settle (no paper flash on load; localStorage still wins; inline script is 1 line, no CSP/Amplify impact) | DONE: node --check OK, judge_demo 6/6, journey 7/7 | commit: 'T16b: lab theme as default identity'
- 2026-09-19 (T16) | Buffy | external design-review triage + theme cycle + hero/editor structural pass | REVIEW WAS AGAINST A STALE SCREENSHOT (described T10-era UI: 'HOW THIS WORKS' eyebrows + native Choose File — both removed in T12/T13); its Concept 3 palette == T14 paper+teal tokens, Concept 1 == T14 dark tokens; took only the genuinely new: lab theme (warm graphite #121110 + rust #d97706 action accent + teal validation, optional third theme via data-theme=lab), theme cycle light→dark→lab with per-theme icons/titles (ph-moon/ph-sun/ph-flask), hero asymmetry 1.12fr→1.35fr, DELETED duplicate tagline under editor (advisor's 'remove, don't restyle'), drag-drop .md onto policy editor (functional affordance + dashed outline state) | DONE: node --check OK, ALL RENDER PATHS OK (DOM stub), judge_demo 6/6, journey 7/7 | commit: 'T16: theme cycle + lab theme, hero/editor structural pass from design review'
- 2026-09-19 (T15) | Buffy | governance bundle + witness nomination + CSV UI + server hardening | actions.py (governance_bundle: 409 without approval, sha256 seal over canonical bytes, BUNDLE_EXPORTED audit; nominate_witness: honesty gate == trace-compare criteria, find_witnesses+validate re-run, WITNESS_NOMINATED audit) + routes on server.py & aws_handlers.py + authz _writable + server.py ValueError→409 on build GETs + strict bind (no SO_REUSEADDR; orphaned backends silently shared :8001 and stole connections) + app.py backend-port failover + backend stderr→.app_backend.log + UI (download button, nominate buttons, CSV card) + docs/traces.md + CHANGELOG + tests 7 new | LIVE-VERIFIED via app runner: 409 refusal, 200+sha after approve, nomination honest 'already covers' path, CSV bulk 2 ingested | DONE | commit: 'T15: governance bundle export, witness nomination, CSV ingest UI; server 409 parity + strict bind + backend port failover'
Format: `- YYYY-MM-DD HH:MM | agent | task | files touched | result | notes/commit-msg-proposal`

- 2026-09-19 23:45 | opencode | T36 deploy-status refresh | `docs/deploy-status.md` (rewrite: verdict table with today's evidence + done-note + current 5-line checklist) | DONE: re-audited live — config.js REAL (https://ssv1hifpvl.execute-api.ap-south-1.amazonaws.com/prod), UI current (fonts/phosphor/theme/palette), auth wall 401 live, CORS echo + Vary correct, auth/config public with real pool. NEW BLOCKER FOUND (curl-green, browser-red): preflight OPTIONS → 401 (authorizer runs; ACAO present but non-2xx preflight fails all browser authed calls) — AWS-documented fix is an unauthenticated OPTIONS /{proxy+} route (infra, flagged for Buffy/human, NOT implemented). Also flagged: /health 401 contradicts auth.md NO_AUTH claim but matches template.yaml (only /, /demo/canonical, /auth/config exempt) — doc-or-template one-liner needed. Callback re-probe unreachable from this network (T35 verdict stands). Remaining: human demo password + cloud_smoke run. Touched NOTHING else; verify 178/178 PASS, pytest 94/94 green | proposed commit: "Deploy status page: fixed verdicts + preflight-401 browser blocker"

- 2026-09-19 23:35 | opencode | T34 deployed demo login docs | `docs/auth.md` (+“Deployed demo login”: sole UserPoolUser demo-admin@processpatch.demo, SUPPRESS, attached to pp-admins via DemoAdminMembership; CFN-can't-set-password → deploy.sh random --permanent print-once block never on disk/git + manual fallback; pp-admins = Gates 2/3 over reviewer abilities; no reviewer demo account; rotate-after-demo guidance) + `docs/demo-script.md` (exactly 2 lines: Beat 0 sign-in header + approved-session sentence) | DONE: every claim grounded in infra/template.yaml lines 129–163 + infra/deploy.sh lines 77–96 (read, not trusted from prompt); NO password invented/committed (none appears in either edit); touched NOTHING else; verify 178/178 PASS, pytest 94/94 green | proposed commit: "Docs: deployed demo login (auth.md) + sign-in beat (demo-script)"

- 2026-09-19 23:20 | opencode | T29 presenter runbook | `docs/demo-script.md` (rewrite: timed 4-min/240s in Judge Mode order — portal 25s, compile 30s, witnesses 30s, procedure 20s, tests 25s, approval 30s, bundle download 25s, nomination 25s, close+buffer 30s; every beat has exact clicks + SAY lines + wrong/fallback, all claims grounded in storyboard shots + judge_demo.py beats 1–6 + actions.py bundle/nomination semantics) | DONE: claimed as T29 (user label T27 collides with Copilot's DONE OIDC row — noted on board); README quick-start VERIFIED current, zero edits (all 8 commands map to real Makefile targets; make app serves :8080 and auto-opens browser per app.py); touched NOTHING else; verify 178/178 PASS, pytest 94/94 green | proposed commit: "Presenter runbook: timed 4-min demo script in Judge Mode order"

- 2026-09-19 23:00 | opencode | T25 live deploy audit | `docs/deploy-status.md` (new: verdict table + exact judge-visible failure + 6-step fix runbook with 4 cited AWS doc URLs + 5-line judge checklist) | DONE: audited https://main.d1bq8os9dwj0d.amplifyapp.com/ from outside — shell 200 OK; config.js CONFIRMED byte-identical default pointing at http://localhost:8000 (judge would see ERR_CONNECTION_REFUSED → misleading local-dev error hint); /api/* 404 (no proxy, by design); deployed shell predates T12 (no fonts/phosphor links); protected-route 401 untestable until API wired. Root causes verified in repo: no amplify.yml/buildspec consumes $API_URL anywhere; deploy.sh already handles ApiUrl output + FrontendOrigin CORS+Cognito wiring, so only the config.js build step is missing (flagged for human/Buffy — NOT implemented, out of scope). Touched NOTHING else; verify 178/178 PASS, pytest 94/94 green | proposed commit: "Deploy audit: Amplify config.js localhost default + fix runbook"

- 2026-09-19 22:10 | opencode | T19 governance docs sweep | `README.md` (API highlights +3 routes; 2-sentence blurbs each for sha256-sealed bundle with 409-without-approval + nomination with honesty-gate/never-by-assertion; "11-tab"→"10-tab" per TABS count, "16 notes"→"21 notes" per glob) + `docs/architecture.md` (bundle/nomination/CSV routes with one-line honesty notes) + `docs/submission.md` (appended "Verify these numbers" block only — T17 sections untouched) | DONE: all route/behavior wording verified against services/api/{server,actions}.py docstrings + dispatch code (bundle_sha256 over canonical bytes, 409 unless approvals on record; nomination requires vs_stale DISAGREE then find_witnesses+validator re-run); number sweep: 178 ✓, 91 ✓ (rerun), 31 (manifest 15/16) ✓, 7 Lambdas ✓, 8 suites ✓; theme names resolved — code identities are light/dark/lab but user-facing names are paper/graphite/lab (lab boot default; neither README nor architecture mentioned themes so nothing stale there) | proposed commit: "Docs: T15 governance routes + honesty notes; stale-count sweep"
- 2026-09-19 22:10 | opencode | T20 CSV demo sample | `demo/traces-sample.csv` (new, 11 rows: eligible/ineligible/7.5-boundary/demo-7.8/witness-B-analogue/backlogs+obc/staff/past-date/8.0-late/just-below + row 11 = deliberate exact duplicate of row 4 to demo idempotent ingest) + `docs/demo-storyboard.md` (Shot 7: sample path + paste moment + duplicates:1 fallback) | DONE: verified LIVE via app on :8082 (isolated .app_data backend) — POST /traces/csv → ingested:10, duplicates:1, zero errors, correct coercion (required.upload_recommendation, ;-split steps, distinct TRC ids); server torn down + .app_data/ deleted after (./data untouched, verified present); no code/frontend touched | proposed commit: "Demo CSV sample (10+1 rows) + storyboard paste moment"
- 2026-09-19 20:00-22:30 | GitHub Copilot | T29-T32 (was T11-T14 on deploy branch) | `infra/template.yaml`, `infra/deploy.sh`, `infra/parameters.json`, `.github/workflows/ci.yml`, `docs/aws-setup.md` | DONE: Cognito WebClient callback URL fixed from invalid `*/` to HTTPS bootstrap origin; first deploy now passes FrontendOrigin explicitly (stacks had retained `*`); AMPLIFY_TOKEN mapped into workflow with fail-fast preflight; second SAM pass hardened with `--resolve-s3` (fixes `S3 Bucket not specified` during auth redeploy) | commits on deploy branch: "Fix Cognito bootstrap callback URL deployment", "Pass HTTPS bootstrap origin on first deploy", "Wire Amplify token into GitHub deployment", "Harden second SAM deployment pass"

- 2026-09-19 23:00 | opencode | T25 live deploy audit | `docs/deploy-status.md` (new: verdict table + exact judge-visible failure + 6-step fix runbook with 4 cited AWS doc URLs + 5-line judge checklist) | DONE: audited https://main.d1bq8os9dwj0d.amplifyapp.com/ from outside — shell 200 OK; config.js CONFIRMED byte-identical default pointing at http://localhost:8000 (judge would see ERR_CONNECTION_REFUSED → misleading local-dev error hint); /api/* 404 (no proxy, by design); deployed shell predates T12 (no fonts/phosphor links); protected-route 401 untestable until API wired. Root causes verified in repo: no amplify.yml/buildspec consumes $API_URL anywhere; deploy.sh already handles ApiUrl output + FrontendOrigin CORS+Cognito wiring, so only the config.js build step is missing (flagged for human/Buffy — NOT implemented, out of scope). Touched NOTHING else; verify 178/178 PASS, pytest 94/94 green | proposed commit: "Deploy audit: Amplify config.js localhost default + fix runbook"

- 2026-09-19 20:00 | GitHub Copilot | T28 (was T10 on deploy branch) | `docs/aws-setup.md`, `agents.md`, AWS IAM role trust | DONE: raw runner log confirmed repository/ref/event are correct; updated trust to allow both legacy `repo:Skullybutcher/Bharatbuilds:*` and immutable `repo:Skullybutcher@*/Bharatbuilds@*:ref:refs/heads/main` subjects; workflow, live trust, infra validation, 178/178 verify, and 70/70 pytest PASS | commit: "Support immutable GitHub OIDC subjects"

- 2026-09-19 19:30 | GitHub Copilot | T27 (was T9 on deploy branch) | `.github/workflows/ci.yml`, `docs/aws-setup.md`, `agents.md` | DONE: added non-secret GitHub repository/ref/event/run diagnostics before OIDC, made STS audience explicit, added run-specific session name; workflow assertion, live trust, infra validation, 178/178 verify, and 70/70 pytest PASS | commit: "Diagnose GitHub OIDC claims before AWS authentication"

- 2026-09-19 21:45 | opencode | T18 handoff to Buffy (no code written) | `agents.md` (board row only) | RELEASED: T18 reassigned to Buffy per human, state OPEN. Handoff context — suggested scope: (1) Command palette = Cmd+K/Ctrl+K fuzzy launcher over TABS + actions (runBuild, cloudRun, toggleJudge, patchDecide, replay…), pure frontend, extend don't break the contract-function list in app.js header comment; (2) Coverage panel recommended = benchmark family × outcome matrix from `/benchmark-runs/latest` (cases carry case_id/split/status; family = id prefix) with dev/eval split coverage — Alternatives logged: per-build validation rollup (Tests tab data) or node coverage (witness-exercised vs total nodes). Constraints for implementer: esc() on every interpolated string, theme tokens in styles.css, keyboard a11y + focus trap for palette dialog, honest-UI (matrix cells map 1:1 to real case statuses); verify with node --check + DOM-stub render + verify.py + pytest + judge_demo | no commit (nothing built)

- 2026-09-19 19:00 | GitHub Copilot | T24 (was T8 on deploy branch) | `.github/workflows/ci.yml`, `docs/aws-setup.md`, `agents.md` | DONE: removed the unreadable `AWS_DEPLOY_ROLE_ARN` secret as a role-selection failure point; workflow now uses verified role ARN and runs `aws sts get-caller-identity`; AWS trust/provider verified; workflow assertion, infra validation, 178/178 verify, and 70/70 pytest PASS | commit: "Use verified IAM role for GitHub OIDC deploy"

- 2026-09-19 18:30 | GitHub Copilot | T23 (was T7 on deploy branch) | `docs/aws-setup.md`, `agents.md`, AWS IAM role trust | DONE: verified GitHub OIDC provider and audience; updated `GitHubActionsProcessPatchDeploy` trust from a single exact ref subject to repository-scoped `repo:Skullybutcher/Bharatbuilds:*`; workflow retains `id-token: write` and `contents: read` | commit: "Broaden GitHub OIDC trust to repository subjects"

- 2026-09-19 18:00 | GitHub Copilot | T22 (was T6 on deploy branch) | `.github/workflows/ci.yml`, `docs/aws-setup.md`, AWS IAM OIDC provider | DONE: registered `https://token.actions.githubusercontent.com` with audience `sts.amazonaws.com`; verified role `GitHubActionsProcessPatchDeploy` trust for `repo:Skullybutcher/Bharatbuilds:ref:refs/heads/main`; upgraded credentials action to v5; infra validation, 178/178 verify, 70/70 pytest, and workflow YAML parse PASS | commit: "Fix GitHub Actions AWS OIDC deployment auth"

- 2026-09-19 21:30 | opencode | T16+T17 docs | `docs/benchmark-v4-proposals.md` (new: 4 families — visa-threshold/list, trial-eligibility intervals+NOT_IN, benefits workreq exception+annual cap, export prohibition+carve-out — each with cited grounding URL, 2 author.py-format scenarios, construct + eval-credibility rationale; all inside rule-ir kinds/operators) + `docs/demo-storyboard.md` (new: 8 shots, 165s + 15s buffer = 180s, per-shot fallback, honesty doctrine, judge_demo gate) + `docs/submission.md` (What-we-built refresh) | DONE: touched NOTHING else (git status confirms); verify 178/178 PASS, pytest 91/91 green | NUMBER CORRECTIONS per verify-rule (task text was snapshot-stale): pytest 84→91 (T15 added 7), docs 19→21 (glob count incl. my 2 new files); also marked stale duplicate T11-OPEN row DONE so finished paths can't be re-claimed | proposed commit: "v0.4 benchmark proposals + 180s demo storyboard + submission refresh"

- 2026-09-19 20:40 | Buffy | T14 identity v4 "ledger of record" | `frontend/styles.css` (token swap: light = warm paper #f6f5f1 + white panels + deep-teal #0f766e accent + dot-grid texture; dark = graphite #0f1115 + muted cyan #3fb5b5; h1 clamp 30-42px editorial scale; spine dots 22px; removed all blue #4c8dff/#2563eb + stale [data-theme=light] selectors) + `frontend/lib/app.js` (applyTheme: paper is DEFAULT, dark opt-in, persists, respects prefers-color-scheme, correct sun/moon icons) | DONE: identity now subject-grounded (governance documents get paper + seal-teal, not devtool blue); zero blue tokens remain; node --check OK, DOM-stub render OK, judge_demo 6/6, verify 178/178, pytest 84/84; structure/contract untouched — pure token+theme logic swap | commit: "UI v4: ledger-of-record identity (paper default, teal seal accent; graphite night theme)"
- 2026-09-19 20:00 | Buffy | T13 filled dashboard canvas | `frontend/styles.css` (editor panel component, activity module grid, util-cluster header grouping, aws-chip sidebar card, spine pulse on live stage, hero focal glow) + `frontend/lib/app.js` (dashHtml/dashFill/dashOne: no-build screen now shows real recent builds/traces/procedure versions from the API with skeleton loaders; sidebar AWS section as a proper card; boot skeleton in index.html) + `frontend/index.html` (header hierarchy: one primary CTA, Cloud/API/theme/sign-in demoted into a quiet util cluster separated by a hairline; initial skeleton before JS boots) | DONE: node --check OK, all tabs render (DOM stub), journey 6/6, judge_demo 6/6, verify 178/178, pytest 84/84, infra PASS; activity modules render ONLY real stored data (skeleton → rows/empty), no decorative filler | commit: "UI v3.1: filled dashboard canvas, editor panel, header hierarchy"
- 2026-09-19 19:30 | Buffy | T12 design-system v3 (applied .agents/skills/frontend-design + design-taste-frontend) | `frontend/fonts/` (Satoshi 400/500/700/900 woff2, self-hosted ~24KB each), `frontend/vendor/phosphor.css` + woff2/woff (icon font, self-hosted), `frontend/styles.css` (v3: sentence-case kickers replace all-caps eyebrows — the top AI tell; gate-spine tracker as the ONE bold element; case-file witness cards; light+dark themes via data-theme tokens; icon nav), `frontend/index.html` (fonts.css, phosphor.css, theme toggle button), `frontend/lib/app.js` (toggleTheme, NAV_ICONS icon nav, gateSpine() state-driven tracker, iconified trust tags + 6 empty states) | DONE: node --check OK, all 10 tabs + hero render error-free (DOM stub), journey 6/6, judge_demo 6/6, verify 178/178; contract functions, esc(), doctrine wording untouched; no external runtime deps (all assets self-hosted — demo-safe offline) | commit: "UI v3: subject-grounded design system (Satoshi, gate spine, case-file witnesses, themes)"
- 2026-09-19 19:10 | opencode | T11 positioning + judge QA | `docs/positioning.md` (new, 514 words: landscape OPA/Celonis/Temporal/Guardrails+Ragas + 4-row comparison table, every competitor claim URL-cited + honest paragraph per README/benchmark/architecture/novelty wording) + `docs/judge-qa.md` (new, 10 Q&A × 3–5 sentences, each repo-grounded in README/architecture/trust-boundary/auth/benchmark) | DONE: zero OPEN QUESTIONs (all technical claims verified against committed docs; non-goals beyond Z3/Neptune deliberately omitted — no committed source); touched NOTHING else (git status confirms only the 2 new files are mine); verify 178/178 PASS, pytest 84/84 green; no README edits needed | proposed commit: "Judge prep docs: positioning landscape + grounded Q&A"

- 2026-09-19 18:40 | Buffy | T10 visual overhaul pass 2 | `frontend/styles.css` (full rewrite: ambient tint, 3-surface elevation + hairline alpha borders, shadows, type scale to 28px + negative tracking, styled file input, borderless hairline tables, hero layout, floating judge dock, skeleton shimmer, custom scrollbars, motion) + `frontend/lib/app.js` (hero markup, **fixed OC's unclosed-quote bug `class="evidence-grid>`** that silently disabled the evidence grid styles, SVG graph palette to current surfaces, Gate-2 checks as honest pass/warn pills instead of raw JSON dump, pipeline-strip emphasis) | DONE: node --check OK, all 10 tabs + hero executed error-free in a DOM-stub harness, journey 6/6, judge_demo 6/6, verify 178/178, pytest 84/84; contract functions, esc(), trust badges, doctrine wording untouched | commit: "UI pass 2: depth/elevation design system, hero layout, honest check pills; fix evidence-grid class bug"
- 2026-09-19 17:20 | Buffy | T7 (seeded demo + app E2E + CSV ingest) + closes opencode's NEEDS_BUFFY | `scripts/app.py` (seed via real API paths + Windows Job Object reaping + port-fallback + own `.app_data/` dir), `services/traces/store.py` (`validate_trace` split out), `services/api/actions.py` (`bulk_ingest_traces_csv`), routes in `server.py`+`aws_handlers.py`, `authz.py` (writable `/traces/csv`), `tests/journey/test_app.py` (rewrite: hermetic + valid orphan check — old version reused the fixture's backend port, invalid; also closes the NEEDS_BUFFY flag), `tests/unit/test_traces_csv.py` (new, 8 tests), `docs/traces.md`, `CHANGELOG.md`, `services/api/server.py` (error-parity fix) | DONE: verify 178/178, pytest 84/84 (+14), INFRA PASS, judge_demo 6/6; server now maps ValueError→409/KeyError→404 like Lambda instead of dropping connections; :8080 blackhole on this machine handled (auto port-fallback, strict bind on Windows) | commit: "T7: seeded demo state, app E2E suite, CSV bulk trace ingest; server error parity"

- 2026-09-19 16:00 | opencode | T9 docs | `docs/demo-script.md` (rewrite: judge_demo gate + local `make app` path + cloud Amplify/SFN path + honesty checklist) + `docs/deployment-runbook.md` (new step 0 `make app` rehearsal, old 0–7 → 1–8) + `CHANGELOG.md` (new v0.3.1: app runner, first-run polish, redesign, docs audit, smoke.ps1 fix) | DONE docs scope; verify.py 178/178 PASS; NO code/frontend touched | !! NEEDS_BUFFY: `pytest tests -q` → 74 tests, 2 FAILED in untracked `tests/journey/test_app.py` (Buffy's new CSV-bulk work, uses ports 8791/8792 so NOT the :8080 conflict): `test_csv_bulk_ingest_via_app` (200 OK but `ingested` 0 == 2, line 126) + `test_shutdown_reaps_backend_child` (Windows FileNotFoundError from subprocess). Uncommitted backend changes present (`services/api/*`, `scripts/app.py`, `test_app.py`). Did not touch per ownership — Buffy please fix before human commits | proposed commit: "Docs for v0.3.1: make-app demo script, runbook step 0, changelog"

- 2026-09-19 15:30 | opencode | T8 first-run polish | `frontend/lib/app.js` only (5 edits; no styles.css change, no new globals, no new files) | DONE: no-build hero → 3-step how-it-works (Compile/Inspect/Approve) + one CTA (runBuild); empty states with sentence+button for Traces-zero (focuses ingest form), Builds-zero (runBuild), Workspaces-zero + Procedures-zero (focus create/register forms), Benchmarks-none (exact `make benchmark` + copy button), Approval-no-build guard; contract intact (node --check OK, all 29 functions + esc() + tokens unchanged); judge_demo ALL 6 BEATS VERIFIED, verify 178/178, pytest 70/70 | ENV NOTE: :8080 is held by MiniTool ShadowMaker AgentService (blackholes HTTP) so `make app` default port can't bind here — verified live on `--port 8082 --backend-port 8002` (shell/config.js=/api/proxy/empty-states all 200, zero-workspace state observed live: 3 traces, 2 builds, 0 workspaces); test server torn down after | proposed commit: "First-run polish: no-build hero + empty states with actions (frontend only)"

- 2026-09-19 15:00 | Buffy | T6 one-command app | `scripts/app.py` (new), `Makefile`, `frontend/lib/app.js` (1 line: sync apiBase from config.js), `README.md` (quickstart) | DONE: `make app` serves UI + /api proxy on ONE port (8080); E2E curl-verified (shell, config.js=/api, styles/app.js 200, health, proxied GET canonical + POST traces, path-traversal guard 404); verify 178/178 + pytest 70/70 after | commit: "Add make app: one-command local app (UI + same-origin API proxy)"
- 2026-09-19 14:10 | Buffy | T5 (NEEDS_BUFFY closure) | `scripts/smoke.ps1` | DONE: mirrored bash smoke auth checks (auth-config fetch, 401 checks on /builds + guardrails via StatusCode helper); PowerShell Parser::ParseFile → parse OK; verify 178/178 PASS after edit | commit: "Smoke (ps1): assert auth-gated routes like bash smoke" — resolves the NEEDS_BUFFY note from T2 review prep
- 2026-09-19 14:00 | Buffy | integration review of T1–T4 | read-only across frontend/**, tests/**, docs/**, CHANGELOG.md | VERDICT: all four tasks verified independently — 178/178 verify, 70/70 pytest (Bob's 18 included), INFRA PASS, judge_demo 6/6 beats, app.js syntax OK, zero banned patterns in styles.css, all 10 tabs + contract functions + esc() (48 uses) intact, Bob's doc numbers spot-checked correct (31-scenario, 16-case, 178, auth cross-refs) | both agents' DONE claims confirmed; tree ready for human to commit
- 2026-09-19 13:30 | opencode | T1 UI upgrade | `frontend/index.html` (shell rewrite) + `frontend/styles.css` (new, design tokens) + `frontend/lib/app.js` (new, all 29 contract functions ported) + `frontend/DESIGN.md` (new) | DONE: verify 178/178 PASS, pytest 70/70, judge_demo ALL 6 BEATS VERIFIED; all 10 tabs + 6-step Judge bar + API paths/bodies + esc() + trust badges + VALIDATED_WITHIN_TESTED_MODEL preserved; no NEEDS_BACKEND gaps | proposed commit: "UI upgrade: design-system pass on frontend (tokens, Stripe-density layout, prune slop, split styles/app.js)"
- 2026-09-19 09:30 | IBM Bob | T2 | `docs/consistency-audit.md` (new); `docs/submission.md`, `docs/benchmark.md`, `docs/deployment-runbook.md`, `docs/architecture.md`, `docs/governance.md`, `docs/trust-boundary.md`, `README.md` (mechanical fixes) | Audit: 11 findings (5 stale counts, 6 missing cross-refs). Mechanical fixes applied: "26-scenario"→"31-scenario" (submission.md); "13-case"→"16-case" eval split + "20"→"22" auto-repaired (benchmark.md); "176/176"→"178/178" (deployment-runbook.md); "176"→"178" (README.md); auth/traces/workspaces cross-refs added to architecture.md, governance.md, trust-boundary.md. Prose-only changes (demo-script.md flow extension) proposed in consistency-audit.md, not auto-applied. | commit: "Docs consistency pass for v0.3.0"
- 2026-09-19 09:00 | IBM Bob | T3 | `tests/unit/test_normalizer_ext.py` (new) | 18 tests, 5 coverage areas: same-field-only delta, interval satisfiability, IN/NOT_IN eval, validate_rule rejections, provenance-only delta. Ran `pytest tests/unit/test_normalizer_ext.py` → 18/18 PASS; full `pytest tests` → 70/70 PASS (52 prior + 18 new). No existing files touched. | commit: "Add normalizer/delta edge-case tests"
- 2026-09-19 08:30 | IBM Bob | T4 | `CHANGELOG.md` (new) | Keep-a-Changelog format; v0.3.0 (auth, traces, language, workspaces, judge demo), v0.2.0 (4 bullets from git log), v0.1.0 (3 bullets). All claims verified against benchmark_runs/BENCH-v030-REF metrics.json + git log. No existing files touched. | commit: "Add CHANGELOG; document v0.3.0"
- 2026-09-19 07:30 | Buffy | setup | `agents.md` | created coordination protocol | task board seeded (T1–T5)
- 2026-09-19 05:00–07:00 | Buffy | (pre-agents.md) auth + traces + language v0.3.0 + workspaces + judge demo | see git status; 178/178 verify, 52/52 pytest, INFRA PASS, bench v0.3.0 all green | 5 commit messages proposed to human; no commits made by agents
