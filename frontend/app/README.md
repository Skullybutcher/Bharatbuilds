# frontend/app — Next.js App Router destination (hackathon: static `index.html` at root)

The deployed demo serves `frontend/index.html` via Amplify (see infra). A
Next.js migration keeps these routes: Overview, Impact, Policy Diff,
Witnesses, Procedure, Patch, Tests, Evidence, Approval, Benchmarks,
Build History — all backed by the same REST surface (`frontend/lib/api.js`).
